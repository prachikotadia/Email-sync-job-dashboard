# JobPulse AI Project

## Project Structure

- **frontend/**: The React application (Vite + Tailwind + Neomorphism UI).
  - Navigate to `frontend/` and run `npm install` and `npm run dev` to start.

## Getting Started

1.  `cd frontend`
2.  `npm install`
3.  `npm run dev`
# Email-sync-job-dashboard
